version: "3.8"

services:
  nornir-mcp:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./examples/conf:/app/conf
      - ./config.yaml:/app/config.yaml
    environment:
      - LOG_LEVEL=INFO
      - MCP_HOST=0.0.0.0
      - MCP_PORT=8000
    restart: unless-stopped
