{
  "napalm_getters": [
    {
      "getter": "facts",
      "description": "Retrieve basic device information (vendor, model, OS version, serial number, uptime)",
      "usage_example": "await napalm_getter(getters=['facts'])"
    },
    {
      "getter": "interfaces",
      "description": "Retrieve physical and logical interface information including status, speed, and description",
      "usage_example": "await napalm_getter(getters=['interfaces'])"
    },
    {
      "getter": "interfaces_ip",
      "description": "Retrieve IP address configuration for all interfaces",
      "usage_example": "await napalm_getter(getters=['interfaces_ip'])"
    },
    {
      "getter": "bgp_neighbors",
      "description": "Retrieve BGP neighbor information and session status",
      "usage_example": "await napalm_getter(getters=['bgp_neighbors'])"
    },
    {
      "getter": "config",
      "description": "Retrieve device configuration (running, startup, candidate)",
      "usage_example": "await napalm_getter(getters=['config'], getters_options={'config': {'retrieve': 'running'}})"
    },
    {
      "getter": "arp_table",
      "description": "Retrieve ARP table with IP to MAC address mappings",
      "usage_example": "await napalm_getter(getters=['arp_table'])"
    },
    {
      "getter": "mac_address_table",
      "description": "Retrieve MAC address table from switches",
      "usage_example": "await napalm_getter(getters=['mac_address_table'])"
    },
    {
      "getter": "network_instances",
      "description": "Retrieve routing table and VRF information",
      "usage_example": "await napalm_getter(getters=['network_instances'])"
    },
    {
      "getter": "bgp_neighbors_detail",
      "description": "Retrieve detailed BGP neighbor information",
      "usage_example": "await napalm_getter(getters=['bgp_neighbors_detail'])"
    },
    {
      "getter": "bgp_config",
      "description": "Retrieve BGP configuration details",
      "usage_example": "await napalm_getter(getters=['bgp_config'])"
    },
    {
      "getter": "lldp_neighbors",
      "description": "Retrieve LLDP neighbor discovery information",
      "usage_example": "await napalm_getter(getters=['lldp_neighbors'])"
    },
    {
      "getter": "lldp_neighbors_detail",
      "description": "Retrieve detailed LLDP neighbor information",
      "usage_example": "await napalm_getter(getters=['lldp_neighbors_detail'])"
    },
    {
      "getter": "users",
      "description": "Retrieve user accounts configured on the device",
      "usage_example": "await napalm_getter(getters=['users'])"
    },
    {
      "getter": "vlans",
      "description": "Retrieve VLAN configuration and status information",
      "usage_example": "await napalm_getter(getters=['vlans'])"
    },
    {
      "getter": "environment",
      "description": "Retrieve environmental information (temperature, power, fans)",
      "usage_example": "await napalm_getter(getters=['environment'])"
    },
    {
      "getter": "ntp_servers",
      "description": "Retrieve NTP server configuration",
      "usage_example": "await napalm_getter(getters=['ntp_servers'])"
    },
    {
      "getter": "snmp_information",
      "description": "Retrieve SNMP configuration details",
      "usage_example": "await napalm_getter(getters=['snmp_information'])"
    },
    {
      "getter": "optics",
      "description": "Retrieve optical transceiver information",
      "usage_example": "await napalm_getter(getters=['optics'])"
    }
  ]
}